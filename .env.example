# 后端 HTTP 主机地址（可选）。未设置时前端回退到 http://localhost:8080
# 直连后端的服务（authApi / feedbackApi）会通过 src/renderer/services/apiConfig.ts 读取该值。
# 复制本文件为 .env 或 .env.local 后按需修改：
VITE_API_BASE_URL=http://localhost:8080
